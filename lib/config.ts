/**
 * Personnalisation du site — voir PERSONNALISATION.md pour le guide complet
 * (couleurs, nom, logo). Modifier ces valeurs suffit à renommer l'app partout
 * (titre de la page, sidebar, e-mails, écrans de connexion).
 */

export const SITE_NAME = "Cadence";

export const SITE_TAGLINE = "Espace de collaboration entre l'agence et ses marques.";

/**
 * Chemin vers un logo image (dans /public), ex: "/logo.svg".
 * Laisser à `null` pour afficher le nom du site en texte (par défaut).
 * Si renseigné, cette image remplace le texte partout (sidebar, écrans de connexion) —
 * pense à choisir un logo qui reste lisible aussi bien sur fond clair que sur fond sombre,
 * ou vois PERSONNALISATION.md pour utiliser deux variantes différentes.
 */
export const LOGO_IMAGE_PATH: string | null = null;
